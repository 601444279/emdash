---
"emdash": minor
"@emdash-cms/admin": minor
---

Adds site-scoped navigation menus. Administrators can assign a menu to a site from **Sites**, while public Astro themes read only that site's internal custom links.

The public menu endpoint excludes external and protocol-relative URLs. Existing menus remain unchanged until assigned to a site.
